#include <iostream>

using namespace std;

int main()
{
    int n;
    cin >> n;
    int* a = (int*)malloc(n * sizeof(int));
    if (a == NULL) { cout << "error"; }
    else
    {
     //your code here
    }
}